{% extends 'base.html.twig' %}

{% block title %}
	Add a new note
{% endblock %}

{% block javascripts %}

{% endblock %}

{% block body %}
 {# {% if app.user is defined %}
 {# rediriger si acces refusé #}
 {# {{ include('components/access-denied.html.twig') }}  #}
 {# access denied
 {% else %} #}

	{{ form(noteForm) }}
  {# {% endif %}    #}
{% endblock %}
