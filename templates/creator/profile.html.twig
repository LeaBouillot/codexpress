{% extends 'base.html.twig' %}

{% block title %}User profile{% endblock %}

{% block body %}
<h1> User profile</h1>
<img src="{{ app.user.image }}" alt="{{ 'profile image of' ~ app.user.username }}" class= "w-16 rounded-full md-4">
{{ dump(app.user) }}
{% endblock %}
